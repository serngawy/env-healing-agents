apiVersion: apps/v1
kind: Deployment
metadata:
  name: env-healing-agents
  namespace: env-healing-agents-ns
  labels:
    app: env-healing-agents
spec:
  replicas: 1
  selector:
    matchLabels:
      app: env-healing-agents
  template:
    metadata:
      labels:
        app: env-healing-agents
    spec:
      serviceAccountName: env-healing-agents

      # -----------------------------------------------------------------------
      # Main container
      # -----------------------------------------------------------------------
      containers:
        - name: env-healing-agents
          # Build: docker build -t env-healing-agents:latest env-healing-agents/
          # Push to your registry then update this field.
          image: ghcr.io/serngawy/env-healing-agents:dev
          imagePullPolicy: Always

          # Run in generic mode with a no-op main process.
          # The agent monitors Kubernetes pod logs via --k8s-label.
          #
          # Namespace modes:
          #   Single namespace    — set WATCH_NAMESPACE_1; leave the rest empty
          #   Multiple namespaces — set WATCH_NAMESPACE_1..4 (empty values are skipped)
          #   All namespaces      — set WATCH_NAMESPACE_1 to "*"
          #
          # Override via Makefile: make deploy WATCH_LABEL=app=my-svc WATCH_NAMESPACE="ns-a ns-b"
          command: ["python", "-m", "env_healing_agent.cli"]
          args:
            # Global flags must come before the subcommand.
            - "--kb-dir"
            - "/kb"
            - "--cms-dir"
            - "/cms"
            - "--k8s-label"
            - "$(WATCH_LABEL)"
            # One --k8s-namespace per watched namespace; empty values are ignored by the CLI.
            # To watch all namespaces set WATCH_NAMESPACE_1 to "*" and clear the rest.
            - "--k8s-namespace"
            - "$(WATCH_NAMESPACE_1)"
            - "--k8s-namespace"
            - "$(WATCH_NAMESPACE_2)"
            - "--k8s-namespace"
            - "$(WATCH_NAMESPACE_3)"
            - "--k8s-namespace"
            - "$(WATCH_NAMESPACE_4)"
            - "--verbose"
            # Subcommand: run a no-op process so the agent stays alive as a monitor.
            - "generic"
            - "sleep"
            - "infinity"

          env:
            # Set to "true" to allow the remediation agent to apply fixes.
            # When "false" (default) issues are detected and diagnosed but no fix is executed.
            - name: REMEDIATION
              value: "false"
            # AI client selection: "claude" (Vertex AI) or "gemini".
            # Only one may be active at runtime. Set the matching credentials below.
            - name: AI_CLIENT
              value: "claude"
            # Gemini — uncomment and populate when AI_CLIENT=gemini.
            # - name: GEMINI_API_KEY
            #   valueFrom:
            #     secretKeyRef:
            #       name: env-healing-agents-gemini
            #       key: api-key
            # - name: GEMINI_MODEL
            #   value: "gemini-2.0-flash"
            - name: WATCH_LABEL
              value: ""
            - name: WATCH_NAMESPACE_1
              value: "default"
            - name: WATCH_NAMESPACE_2
              value: "kube-system"
            - name: WATCH_NAMESPACE_3
              value: ""
            - name: WATCH_NAMESPACE_4
              value: ""
            - name: PYTHONUNBUFFERED
              value: "1"
            # Pod's own namespace — used by _sync_to_configmap to patch the right namespace
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            # ConfigMap names the agent will patch when it persists new knowledge at runtime
            - name: KNOWN_ISSUES_CONFIGMAP
              value: "env-healing-agents-known-issues-1"
            - name: FIX_STRATEGIES_CONFIGMAP
              value: "env-healing-agents-fix-strategies-1"
            - name: REMEDIATION_OUTCOMES_CONFIGMAP
              value: "env-healing-agents-remediation-outcomes-1"
            - name: ANTHROPIC_VERTEX_PROJECT_ID
              valueFrom:
                secretKeyRef:
                  name: env-healing-agents-vertex
                  key: project-id
            - name: CLOUD_ML_REGION
              valueFrom:
                secretKeyRef:
                  name: env-healing-agents-vertex
                  key: region
            # AWS credentials file mounted from secret
            - name: AWS_SHARED_CREDENTIALS_FILE
              value: "/root/.aws/credentials"
            # OCM credentials for rosa login + rosa create ocm-role fix strategy
            - name: OCM_API_URL
              valueFrom:
                secretKeyRef:
                  name: env-healing-agents-ocm-credentials
                  key: ocmApiUrl
            - name: OCM_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: env-healing-agents-ocm-credentials
                  key: ocmClientID
            - name: OCM_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: env-healing-agents-ocm-credentials
                  key: ocmClientSecret
            # GCP Service Account key for Vertex AI authentication
            - name: GOOGLE_APPLICATION_CREDENTIALS
              value: "/gcp/sa-key.json"

          volumeMounts:
            # Merged knowledge base — written at startup by merge_kb.merge_knowledge_base()
            - name: knowledge-base
              mountPath: /kb
            # ConfigMap chunks merged at startup into /kb
            - name: known-issues-1
              mountPath: /cms/known-issues/1
            - name: fix-strategies-1
              mountPath: /cms/fix-strategies/1
            - name: remediation-outcomes-1
              mountPath: /cms/remediation-outcomes/1
            # GCP SA key for Vertex AI ADC
            - name: gcp-sa
              mountPath: /gcp
              readOnly: true
            # AWS credentials file
            - name: aws-credentials
              mountPath: /root/.aws
              readOnly: true
            # Uncomment to enable FileTailStream on a host log directory
            # - name: host-test-logs
            #   mountPath: /host/var/log/my-test-runner
            #   readOnly: true
            # Uncomment to enable JournaldStream (pass journal_path to the stream)
            # - name: host-journal
            #   mountPath: /host/var/log/journal
            #   readOnly: true

          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi

          livenessProbe:
            exec:
              command: ["pgrep", "-f", "env_healing_agent.cli"]
            initialDelaySeconds: 15
            periodSeconds: 30
            failureThreshold: 3

          readinessProbe:
            exec:
              command: ["python", "-c", "import env_healing_agent"]
            initialDelaySeconds: 10
            periodSeconds: 15

      # -----------------------------------------------------------------------
      # Volumes
      # -----------------------------------------------------------------------
      volumes:
        # emptyDir populated at startup by merge_kb.merge_knowledge_base()
        - name: knowledge-base
          emptyDir: {}

        # known_issues chunks — add more here as the file grows
        - name: known-issues-1
          configMap:
            name: env-healing-agents-known-issues-1
        # fix_strategies chunks
        - name: fix-strategies-1
          configMap:
            name: env-healing-agents-fix-strategies-1

        # remediation_outcomes chunks
        - name: remediation-outcomes-1
          configMap:
            name: env-healing-agents-remediation-outcomes-1

        # GCP Service Account key for Vertex AI authentication
        - name: gcp-sa
          secret:
            secretName: env-healing-agents-gcp-sa

        # AWS credentials file
        - name: aws-credentials
          secret:
            secretName: env-healing-agents-aws-credentials

        # -----------------------------------------------------------------------
        # Optional: FileTailStream — mount a host log file or emptyDir log path.
        # Add a volumeMount to the main container at the same mountPath and pass
        # --tail-file <mountPath>/<filename> in the args.
        # -----------------------------------------------------------------------
        # - name: host-test-logs
        #   hostPath:
        #     path: /var/log/my-test-runner
        #     type: DirectoryOrCreate

        # -----------------------------------------------------------------------
        # Optional: JournaldStream — mount the host systemd journal so
        # journalctl --directory can read it from inside the pod.
        # Then pass journal_path="/host/var/log/journal" when constructing
        # JournaldStream, or add --journald-unit to the CLI args.
        #
        # Also requires hostPID: true and may need a permissive SCC/AppArmor
        # profile depending on your cluster's security policy.
        # -----------------------------------------------------------------------
        # - name: host-journal
        #   hostPath:
        #     path: /var/log/journal
        #     type: DirectoryOrCreate

      # Uncomment if using JournaldStream (needed to read host journal socket)
      # hostPID: true

      restartPolicy: Always
